{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run coin_counter",
        "type": "python",
        "request": "launch",
        "program": "${workspaceFolder}/src/coin_counter.py",
        "console": "integratedTerminal",
        "args": [
          "--input", "${workspaceFolder}/data/sample.jpg",
          "--outdir", "${workspaceFolder}/out",
          "--tol", "2.5",
          "--ref_mode", "largest"
        ]
      }
    ]
  }
  